# G10-FX-Regime-detector
Uses a Gaussian Hidden Markov Model (HMM) to classify the macro environment into distinct regimes based on observable market indicators, then shows which G10 FX trades have historically outperformed in each regime.
